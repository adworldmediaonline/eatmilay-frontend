# Backend API base URL (auth client appends /api/auth)
NEXT_PUBLIC_API_URL=http://localhost:3005

# Razorpay (client-side checkout - use test key for development)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxx

# Shiprocket pickup pincode (for shipping rates API)
NEXT_PUBLIC_SHIPROCKET_PICKUP_POSTCODE=302017

# Note: Add FRONTEND_USER_URL=http://localhost:3001 to backend .env
# when running the user frontend on port 3001.
